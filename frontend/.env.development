# 本地开发时把 BASE_URL 留空，走 Next.js dev server 的 rewrites() 代理
# （见 next.config.js：/api/** 和 /knowledge-base/** 会被转发到 rag-gateway）。
# 这样 fetch 都是 same-origin (http://localhost:3000)，不用处理 CORS。
#
# 如果要让浏览器**直连** gateway（比如想在 Swagger / Postman 之外跑脱离 Next
# 的测试页），改成 NEXT_PUBLIC_API_URL=http://127.0.0.1:8080，但那样需要
# gateway 侧开 CORS（Spring Cloud Gateway 的 spring.cloud.gateway.globalcors）。
NEXT_PUBLIC_API_URL=
