## Codebase Patterns
- nacos-config/ 整目录在 .gitignore（第 64 行）中被忽略；配置以 Nacos 控制台为真实源，仓库里的 yaml 仅作参考/同步用，不要期望 git 追踪
- rag-ai-service 用 uv 管理依赖（见 pyproject.toml / uv.lock），跑脚本用 `uv run python ...`；requirements.txt 保留作为 Docker 兼容，由 `uv export` 生成
- app/core/config.py 的 env_file 用 `_SERVICE_ROOT / ".env"` 绝对路径，避免 uvicorn 或测试在任意 cwd 下读不到 .env
- Settings 字段优先从 os.getenv(...) 取，缺省值作为 pydantic-settings 的 fallback；给字段改默认值时注意两层都要改
- 本地 .env 是 Settings 的真正来源；nacos-config/*.yaml 是运行时热更新快照，敏感信息（OPENAI_API_KEY 等）只进 .env，不进 Nacos
- Java 侧敏感配置用 Spring 占位符 `${ENV_VAR:}` 从环境变量注入，nacos-config 里不写明文；bean 创建处再做 `StringUtils.hasText` 校验 + fail-fast
- Maven 构建顺序：`rag-service`(parent pom) → rag-common → rag-common-service → 业务模块；缺 parent pom 时先 `./mvnw -N -DskipTests install -f pom.xml` 装根 pom
- `-DskipTests` 只跳过执行不跳过编译；要彻底跳过测试编译用 `-Dmaven.test.skip=true`。业务模块必须显式依赖 spring-boot-starter-test(test scope)，否则 src/test 骨架类连编译都过不了
- ragwebui schema 真实源：.dockervolumes/mysql/sql/01_schema.sql（由 alembic head 编译而成）+ alembic 自身；二者须同步演进。01_schema.sql 末尾 `alembic_version` 种入当前 head，保证容器首启后 `alembic upgrade head` 是 no-op
- docker-entrypoint-initdb.d 下的 SQL 只在 mysql data 目录为空时触发；已经有数据的环境改 schema 需手动 `docker exec -i <db> mysql -uroot -proot < file.sql`（CREATE TABLE IF NOT EXISTS 可保证幂等）
- Feign 客户端路径 = 目标 controller 的 `@RequestMapping` + 方法 `@GetMapping/@PostMapping`；只写方法级路径会被网关当成根路径匹配→直连服务时也对不上。审 Feign 路径务必对照目标 controller 的类级 RequestMapping

---

## 2026-05-07 - US-002
- 新建 nacos-config/rag-ai-service.yaml（server.port=8000 + mysql/minio/chromadb/document 各段，127.0.0.1 本机直连，文件被 .gitignore 忽略，仅作参考）
- 编辑 rag-ai-service/app/core/config.py：
  - NACOS_DATA_ID 默认值 'rag-gateway.yaml' → 'rag-ai-service.yaml'
  - NACOS_SERVER_ADDR 默认值 'localhost:8848' → '127.0.0.1:8848'
  - env_file 改用 `_SERVICE_ROOT / ".env"` 绝对路径锁定 rag-ai-service/.env
- 新建 rag-ai-service/.env.example：覆盖 MYSQL_*、CHROMA_DB_*、MINIO_*、NACOS_* 五项，并预留 US-003 的 LLM/Embedding 变量占位
- 质量检查：`uv run python -c "from app.core.config import settings; assert settings.NACOS_DATA_ID == 'rag-ai-service.yaml'"` 退出码 0
- 文件变更：
  - A nacos-config/rag-ai-service.yaml（gitignored，本地参考）
  - M rag-ai-service/app/core/config.py
  - A rag-ai-service/.env.example
- **Learnings for future iterations:**
  - nacos-config/ 被 .gitignore 忽略，是故意的：配置真实源在 Nacos 控制台，不要为了"让 git 看到"去改 .gitignore。提交信息里写明"已发布到 Nacos 控制台 / 本地已备份 yaml"即可
  - pydantic BaseSettings 的 env_file 用相对路径在多 cwd 场景下很脆弱；统一用 `Path(__file__).resolve().parents[N]` 推算服务根目录更稳
  - US-002 的 PRD 验收要求里"`python app/main.py` 可见 rag-ai-service 在 Nacos 服务列表"依赖真实 Nacos 实例；当前迭代只覆盖了静态配置 + Typecheck，活体注册留给 US-012（start-all + check-nacos 脚本）统一验证
---

## 2026-05-07 - US-003
- 编辑 nacos-config/rag-document-service.yaml：api-key 从明文 'your-openai-api-key-here' 改成 `${OPENAI_API_KEY:}`，加注释说明必须从环境变量注入
- 重写 rag-document-service/.../config/AIConfig.java：
  - `@Value` 改成 `${ai.embedding-model.api-key:}`（缺省空串，不 fail on missing property）
  - bean 方法内用 `StringUtils.hasText(apiKey)` 校验，空值抛 IllegalStateException，错误消息明确指向 OPENAI_API_KEY / Nacos 覆盖
- 修补 rag-document-service/pom.xml：补 `spring-boot-starter-test` (test scope)，否则 src/test 下的 ApplicationTests 骨架类连 testCompile 都过不去，`-DskipTests` 只跳过执行不跳过编译
- rag-ai-service/.env.example 已在 US-002 覆盖 OPENAI_* / DASH_SCOPE_* / OLLAMA_* 占位，本迭代无需再动
- 质量检查：
  - `cd rag-common && ./mvnw -q -DskipTests install` 退出 0
  - `cd rag-common-service && ./mvnw -q -DskipTests install` 退出 0
  - `cd rag-document-service && ./mvnw -DskipTests package` 退出 0（BUILD SUCCESS，产物 target/rag-document-service-1.0.0.jar）
- 文件变更：
  - M nacos-config/rag-document-service.yaml（gitignored，仅本地参考）
  - M rag-document-service/src/main/java/com/chuhezhe/ragdocumentservice/config/AIConfig.java
  - M rag-document-service/pom.xml
- **Learnings for future iterations:**
  - 首次跑任意 Java 模块 mvnw 之前，必须按顺序装好 parent pom + rag-common + rag-common-service，否则 "Could not find artifact com.chuhezhe:rag-common-service:jar:1.0.0" 或 "rag-service:pom:1.0.0 was not found"。装 parent 用 `./mvnw -N -DskipTests install -f pom.xml`
  - `-DskipTests` 与 `-Dmaven.test.skip=true` 有区别：前者编译测试但不跑，后者连编译都跳。PRD 用的是前者，所以 src/test 里的类必须能编译通过 → 需要 spring-boot-starter-test 依赖
  - Spring 占位符 `${VAR:default}` 里冒号后是默认值；`${OPENAI_API_KEY:}` 表示缺省为空串，适合用在"希望从环境变量注入，但允许暂时缺失并在 bean 初始化时统一校验"的场景
  - Maven 中央仓库偶尔握手失败（"Remote host terminated the handshake"），直接重试一次通常就好，不要改动 settings.xml
---

## 2026-05-07 - US-004
- 选择 approach (b) MySQL init SQL：新建 .dockervolumes/mysql/sql/01_schema.sql
  - 覆盖 PRD 列出的 9 张表 + `messages`（alembic head 含但 PRD 9 张表清单漏列，chat 流程需要）+ `alembic_version`
  - 每条 CREATE TABLE 用 IF NOT EXISTS，可重复执行幂等
  - 所有 FK / UNIQUE / 普通 KEY 显式命名，方便 US-008 做 schema diff
  - 表尾 `INSERT IGNORE INTO alembic_version VALUES ('3580c0dcd005')` 把当前 head 写入，首启后再跑 `alembic upgrade head` 是 no-op
  - approach (a)（先起 Python 跑 alembic）被否掉：Java 服务若等 Python 先启会引入不必要的启动耦合，失败面更大
- 脚本挂载路径不需要改：docker-compose.dev.yml 已把 `./.dockervolumes/mysql/sql:/docker-entrypoint-initdb.d/` 挂上，文件放进去即可
- 质量检查：
  - Dry-run：`docker exec rag-service-db-1 mysql -uroot -proot -e "CREATE DATABASE ragwebui_dryrun"` + 把 `USE ragwebui` 改写成 `USE ragwebui_dryrun` 灌入，DDL 全部解析成功
  - 对已经存在的 ragwebui 库 apply：`docker exec -i rag-service-db-1 mysql -uroot -proot < 01_schema.sql` 退出 0；`show tables from ragwebui` 返回 11 行（10 业务表 + alembic_version），覆盖 PRD 的 9 张表
- 文件变更：
  - A .dockervolumes/mysql/sql/01_schema.sql（gitignored? 检查下 .gitignore 对 .dockervolumes 的覆盖）
- **Learnings for future iterations:**
  - documents.file_size 在 alembic initial 是 sa.Integer()，但 Python ORM app/models/knowledge.py 已经写 BigInteger → 两边不一致。US-004 严格跟 alembic head 留 INT，US-008 去把它改成 BIGINT 并同步 migration + 01_schema.sql
  - PRD 列的"9 张表"漏了 messages；如果只按 9 张表出 SQL，chat 功能一定会挂。凡是按 alembic head 生成 schema 的任务，要以 migration 串为准、PRD 清单为辅
  - 以后每新增一条 alembic migration：必须同步把 DDL 补进 01_schema.sql，并把 alembic_version 种入值改成新的 head revision id，否则首启后 `alembic upgrade head` 会把 migration 重跑一遍
  - docker-entrypoint-initdb.d 在 data 目录非空时不跑；想验证 init 链路得先清 `.dockervolumes/mysql/data` 然后重启容器
---

## 2026-05-08 - US-005
- 编辑 rag-document-service/.../feign/KBClient.java：`@GetMapping("/{kb_id}")` → `@GetMapping("/knowledge-base/{kb_id}")`
  - 原路径缺少类级前缀，实际转发到 rag-knowledge-service 时拿到的是 `GET /{kb_id}`，对不上 KnowledgeBaseController 的 `@RequestMapping("/knowledge-base") + @GetMapping("/{kb_id}")`，必然 404
  - 方法注释同步补上，写清完整路径由来
- 质量检查：`cd rag-document-service && ./mvnw -DskipTests package` 退出码 0（BUILD SUCCESS）
- 文件变更：
  - M rag-document-service/src/main/java/com/chuhezhe/ragdocumentservice/feign/KBClient.java
- **Learnings for future iterations:**
  - Spring Cloud Feign 客户端的路径匹配 target controller 的"类级 @RequestMapping + 方法级 @XxxMapping"拼出来的完整路径；写 Feign 时要对着 target controller 类头看，不能只看方法签名
  - 后续审其他 Feign client（如 DocumentServiceClient）时同样要按这条规则逐一对照，US-006 就是类似的路径对齐问题
  - POST /api/ai/documents/process 之前返回 404 的根因就是这里；端到端活体验证留给 US-013 的 e2e-smoke.sh
---
