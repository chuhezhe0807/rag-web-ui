{
  "project": "rag-service",
  "branchName": "ralph/microservice-fix",
  "description": "Fix the post-microservice-split RAG project so the full pipeline (register → login → KB → upload → process → chat with citations) runs end-to-end across the Java services (user/document/knowledge/gateway on Spring Cloud + Nacos) and the Python rag-ai-service (FastAPI). Infrastructure (MySQL/Nacos/ChromaDB/MinIO) is already up via docker compose.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Add missing Python deps to rag-ai-service/requirements.txt",
      "description": "As a developer, I want rag-ai-service to install cleanly so the FastAPI process can import all modules at startup.",
      "acceptanceCriteria": [
        "rag-ai-service/requirements.txt contains: loguru, pydantic-settings, nacos-sdk-python (>=2.0.0, the v2.nacos API), httpx, pyyaml",
        "In a clean venv: `pip install -r rag-ai-service/requirements.txt` succeeds",
        "From rag-ai-service/: `python -c \"import app.main\"` exits 0 with no ImportError",
        "Build/Typecheck passes (python -c \"import app.main\" returns 0)"
      ],
      "priority": 1,
      "passes": true,
      "notes": "切到 uv 管理：新建 pyproject.toml + uv.lock；requirements.txt 改由 uv export 生成（保留给 Docker 兼容）。Dockerfile / Dockerfile.dev 同步切到 uv sync --frozen。verified: uv run python -c \"import app.main\" 退出码 0；v2.nacos 命名空间可用（nacos-sdk-python 3.2.0）。"
    },
    {
      "id": "US-002",
      "title": "Add rag-ai-service Nacos config and local env defaults",
      "description": "As an operator, I want rag-ai-service to load its own Nacos config and run locally against host-mapped infra so it stops reading rag-gateway.yaml by mistake.",
      "acceptanceCriteria": [
        "Create nacos-config/rag-ai-service.yaml with at least: server.port=8000, mysql (host=127.0.0.1, port=3306, db=ragwebui, user=ragwebui, password=ragwebui), minio (endpoint=127.0.0.1:9000, access/secret=minioadmin, bucket=documents), chromadb (host=127.0.0.1, port=8001)",
        "Update rag-ai-service/app/core/config.py default NACOS_DATA_ID from 'rag-gateway.yaml' to 'rag-ai-service.yaml'",
        "Add rag-ai-service/.env.example with MYSQL_SERVER=127.0.0.1, CHROMA_DB_HOST=127.0.0.1, CHROMA_DB_PORT=8001, MINIO_ENDPOINT=127.0.0.1:9000, NACOS_SERVER_ADDR=127.0.0.1:8848",
        "Manual verification step in commit message: `python app/main.py` starts and `rag-ai-service` is visible in Nacos http://127.0.0.1:8848/nacos service list",
        "Build/Typecheck passes (python -c \"from app.core.config import settings; assert settings.NACOS_DATA_ID == 'rag-ai-service.yaml'\")"
      ],
      "priority": 2,
      "passes": true,
      "notes": "nacos-config/ 在 .gitignore 中被整目录忽略（配置入 Nacos 后仅作本地参考），故 rag-ai-service.yaml 未进 git；config.py 同步把 NACOS_SERVER_ADDR 默认值从 localhost:8848 改为 127.0.0.1:8848，env_file 用 _SERVICE_ROOT 绝对路径锁定，避免 cwd 漂移导致读不到 .env。verified: uv run python -c 断言 NACOS_DATA_ID=='rag-ai-service.yaml' 退出码 0。"
    },
    {
      "id": "US-003",
      "title": "Wire LLM/Embedding API keys on Python and Java sides",
      "description": "As an operator, I want valid embedding/LLM credentials on both sides so the document pipeline and chat actually call the model.",
      "acceptanceCriteria": [
        "rag-ai-service/.env.example documents OPENAI_API_KEY (and optional OPENAI_API_BASE / OPENAI_MODEL / OPENAI_EMBEDDINGS_MODEL / DASH_SCOPE_API_KEY / OLLAMA_API_BASE)",
        "nacos-config/rag-document-service.yaml replaces 'your-openai-api-key-here' with a config placeholder pulled from environment variable (e.g. ${OPENAI_API_KEY:}) so secrets are not committed literally",
        "rag-document-service/src/main/java/com/chuhezhe/ragdocumentservice/config/AIConfig.java fails fast with a clear error message when api-key is blank, instead of silently constructing a broken bean",
        "Build/Typecheck passes (cd rag-document-service && ./mvnw -DskipTests package)"
      ],
      "priority": 3,
      "passes": true,
      "notes": ".env.example 在 US-002 已经铺好 OPENAI_* / DASH_SCOPE_* / OLLAMA_* 占位；本迭代：nacos-config/rag-document-service.yaml 里 api-key 改成 ${OPENAI_API_KEY:}（Spring 占位符），AIConfig 在 bean 初始化时用 StringUtils.hasText 做 fail-fast（抛 IllegalStateException + 清晰提示）。顺手修复 pom.xml 缺 spring-boot-starter-test 导致 `./mvnw -DskipTests package` 在 testCompile 阶段失败的历史坑，否则后续 US-005/006/011 的 PRD build 命令也跑不过。verified: ./mvnw -DskipTests package 退出码 0。"
    },
    {
      "id": "US-004",
      "title": "Initialize ragwebui database tables from a single source of truth",
      "description": "As a developer, I want a deterministic way to create all 7 application tables so Java services do not crash on startup waiting for Python alembic.",
      "acceptanceCriteria": [
        "Document the chosen approach in scripts/ralph/progress.txt: either (a) startup-order: rag-ai-service must boot first to run alembic upgrade head, or (b) extract DDL into .dockervolumes/mysql/sql/01_schema.sql so MySQL initializes the schema on first boot",
        "If approach (b) is chosen: produce 01_schema.sql covering users, knowledge_bases, documents, document_uploads, processing_tasks, document_chunks, api_keys, chats, chat_knowledge_bases — generated from the alembic head schema",
        "Manual verification step in commit message: `docker exec rag-service-db-1 mysql -uroot -proot -e \"show tables from ragwebui\"` lists all 9 tables",
        "Build/Typecheck passes (sql file parses with `mysql --help` style sanity check, or `python -c \"import alembic\"` for approach (a))"
      ],
      "priority": 4,
      "passes": true,
      "notes": "选 approach (b)：.dockervolumes/mysql/sql/01_schema.sql，由 alembic head 3580c0dcd005 的 upgrade 串编译而成，覆盖 PRD 列出的 9 张表 + messages（alembic head 含但 PRD 漏列）+ alembic_version 种入 '3580c0dcd005'，后续 `alembic upgrade head` 为 no-op。已在临时库 dryrun 导入通过并在 ragwebui 直接 apply，`show tables from ragwebui` 返回 11 行（10 业务 + alembic_version），覆盖 PRD 9 张表；documents.file_size 暂时保留 INT（与 alembic head 一致），留给 US-008 改 BIGINT 并同步 migration + 01_schema.sql。注意：docker-entrypoint-initdb.d 只在 mysql 首次 init 时跑，.dockervolumes/mysql/data 已存在时不会触发；重装验证需先清 data 目录。"
    },
    {
      "id": "US-005",
      "title": "Fix KBClient feign path mismatch (404 on /api/ai/documents/process)",
      "description": "As the document service, I want my call to rag-knowledge-service to hit the real controller path so document processing can read knowledge-base info.",
      "acceptanceCriteria": [
        "Edit rag-document-service/src/main/java/com/chuhezhe/ragdocumentservice/feign/KBClient.java",
        "Change @GetMapping(\"/{kb_id}\") to @GetMapping(\"/knowledge-base/{kb_id}\") to match KnowledgeBaseController.@RequestMapping(\"/knowledge-base\")",
        "Manual verification step in commit message: POST /api/ai/documents/process returns 200/202 (no longer 404 from KBClient)",
        "Build/Typecheck passes (cd rag-document-service && ./mvnw -DskipTests package)"
      ],
      "priority": 5,
      "passes": true,
      "notes": "@GetMapping(\"/{kb_id}\") → @GetMapping(\"/knowledge-base/{kb_id}\")，与 rag-knowledge-service KnowledgeBaseController 的 @RequestMapping(\"/knowledge-base\") + @GetMapping(\"/{kb_id}\") 拼出的完整路径一致。verified: ./mvnw -DskipTests package 退出码 0；端到端 404 → 200 验证留给 US-013 smoke 脚本。"
    },
    {
      "id": "US-006",
      "title": "Reconcile /api/ai/documents/upload/record endpoint with KB upload flow",
      "description": "As the knowledge service, I want my Feign call for upload records to hit a real endpoint so PUT /knowledge-base/{id}/documents/upload does not 404 mid-flow.",
      "acceptanceCriteria": [
        "Pick option A or B and document the choice in commit body",
        "Option A: rag-document-service/.../controller/DocumentController.java adds @PostMapping(\"/upload/record\") that delegates to documentUploadService.addUploadRecord and returns Result<Integer>",
        "Option B: rag-knowledge-service/.../service/KnowledgeBaseService.uploadKnowledgeBaseDocuments stops calling addUploadDocumentRecord and relies solely on /api/ai/documents/upload (which already creates the upload record); also remove the now-dead method from rag-knowledge-service/.../feign/DocumentServiceClient.java",
        "Manual verification: PUT /knowledge-base/{kb_id}/documents/upload returns success and SELECT * FROM document_uploads shows a new row",
        "Build/Typecheck passes (cd rag-document-service && ./mvnw -DskipTests package && cd ../rag-knowledge-service && ./mvnw -DskipTests package)"
      ],
      "priority": 6,
      "passes": true,
      "notes": "选 Option B：/api/ai/documents/upload 已经在落 MinIO 的同一流程里调 documentUploadService.addUploadRecord 并把主键回写到 UploadDocResult.uploadDocRecordId；Option A 再开一个端点会产生重复 document_uploads 记录。改动：KnowledgeBaseService.uploadKnowledgeBaseDocuments 读 uploadRes.getData().getUploadDocRecordId()；删 DocumentServiceClient.addUploadDocumentRecord + 相关 UploadDocRecordDTO/BigInteger import；顺手把 rag-knowledge-service/pom.xml 也补上 spring-boot-starter-test（与 US-003 对 rag-document-service 做的修复一致）。verified: 两边 ./mvnw -DskipTests package 均退出 0；document_uploads 新增行的活体校验留给 US-013。"
    },
    {
      "id": "US-007",
      "title": "Remove duplicate KB CRUD/upload/process from rag-ai-service (Python)",
      "description": "As the architect, I want only Java rag-knowledge-service / rag-document-service to own KB CRUD and document ingestion so the Python side is solely responsible for retrieval, chat, openapi, api-keys.",
      "acceptanceCriteria": [
        "Delete from rag-ai-service/app/api/api_v1/knowledge_base.py: POST '', GET '', GET '/{kb_id}', PUT '/{kb_id}', DELETE '/{kb_id}', POST '/{kb_id}/documents/upload', POST '/{kb_id}/documents/preview', POST '/{kb_id}/documents/process', GET '/{kb_id}/documents/tasks', GET '/{kb_id}/documents/{doc_id}', POST '/cleanup'",
        "Keep POST '/test-retrieval' and any internal helper used by chat",
        "Edit nacos-config/rag-gateway.yaml: remove route 'rag-ai-service-knowledge-base' (Path=/api/ai/knowledge-base/**); leave the Java route '/knowledge-base/**' as the single KB entrypoint",
        "Manual verification: GET /api/ai/knowledge-base via gateway returns 404; GET /knowledge-base via gateway returns the user's KB list",
        "Build/Typecheck passes (python -c \"from app.api.api_v1 import knowledge_base\" succeeds; python -c \"from app.api.api_v1.knowledge_base import router; assert all(r.path != '/' for r in router.routes if r.methods == {'GET'})\")"
      ],
      "priority": 7,
      "passes": true,
      "notes": "knowledge_base.py 整文件重写，只保留 POST /test-retrieval（chat 检索调用要用）和必要 import；原 add_processing_tasks_to_queue 等辅助函数跟着被删的 endpoint 一起清掉（无跨文件引用）。nacos-config/rag-gateway.yaml 删掉 rag-ai-service-knowledge-base 路由，加注释解释 Java route 是 KB 单一入口。test-retrieval 仍挂在 /api/ai/knowledge-base/test-retrieval（因为 api.py 里 prefix 还是 /knowledge-base），但网关只保留 Java 路由 /knowledge-base/**，所以外部走网关只能打到 Java；chat 服务在 rag-ai-service 进程内直接调用 router 不受影响。verified: `uv run python -c 'from app.api.api_v1 import knowledge_base'` 退出 0；`assert all(r.path != '/' for r in router.routes if r.methods == {'GET'})` 通过（唯一 route 是 POST /test-retrieval）；`uv run python -c 'import app.main'` 退出 0。"
    },
    {
      "id": "US-008",
      "title": "Align Python ORM and Java MyBatis entity column types",
      "description": "As a developer, I want consistent column types across both sides so writes do not overflow and reads do not fail deserialization.",
      "acceptanceCriteria": [
        "Audit 7 tables: users, knowledge_bases, documents, document_uploads, processing_tasks, document_chunks, api_keys",
        "Confirm/fix documents.file_size as BIGINT to match Java Document.fileSize (BigInteger); update rag-ai-service/app/models/knowledge.py and add an alembic migration if a column type change is needed",
        "Produce scripts/ralph/check_schema.sql: a script that SELECTs information_schema.COLUMNS for those 7 tables and prints column_name + data_type + is_nullable",
        "Add scripts/ralph/expected_schema.md: a table per entity comparing Python ORM column, Java @TableField, and MySQL actual",
        "Manual verification: running `mysql -uroot -proot ragwebui < scripts/ralph/check_schema.sql` matches expected_schema.md with zero diffs",
        "Build/Typecheck passes (cd rag-document-service && ./mvnw -DskipTests package && python -c \"from app.models.knowledge import Document\")"
      ],
      "priority": 8,
      "passes": true,
      "notes": "documents.file_size: Python ORM 已经是 BigInteger NOT NULL，Java Document.fileSize 也是 BigInteger，只有 MySQL 从 alembic initial 继承的 INT NULL 对不上。补上 alembic migration a1b2c3d4e5f6_align_documents_file_size_bigint（down_revision=3580c0dcd005，upgrade: alter_column INT → BigInteger NOT NULL），同步更新 01_schema.sql 的 DDL 与 alembic_version seed 到新 head；对现有 ragwebui 已手工 ALTER 并 UPDATE alembic_version，check_schema.sql 输出 file_size = bigint NO（NOT NULL）✓。产物：scripts/ralph/check_schema.sql（打印 7 张表 column_name/data_type/is_nullable 等）、scripts/ralph/expected_schema.md（Python vs Java vs MySQL 对照 + 末尾列出已知遗留 deltas 作为后续 backlog）。verified: ./mvnw -DskipTests package 退出 0；`from app.models.knowledge import Document` 打印 BIGINT / nullable=False；mysql < check_schema.sql 结果与 expected_schema.md 各表一致。"
    },
    {
      "id": "US-009",
      "title": "Wire JWT propagation: Gateway → Java services → Feign",
      "description": "As an authenticated user, I want my token to flow through gateway and downstream feign calls so protected endpoints see my identity.",
      "acceptanceCriteria": [
        "Verify rag-common-service/.../config/WebMvcConfig.java registers AuthInterceptor via addInterceptors and excludes /api/ai/auth/** + any @AnonymousAccess paths",
        "rag-common-service/.../config/FeignConfiguration.java provides a feign.RequestInterceptor bean that copies the X-Token / Authorization header from the inbound request (RequestContextHolder) to the outbound feign request",
        "Add a GlobalFilter to rag-gateway: validate JWT presence (skip /api/ai/auth/**), reject with 401 + Result.error if missing/invalid, otherwise let the request pass with the original header intact",
        "Manual verification: `curl -H 'X-Token: <valid>' :8080/knowledge-base` returns 200 with the caller's KBs; `curl :8080/knowledge-base` returns 401",
        "Build/Typecheck passes (cd rag-common-service && ./mvnw -DskipTests package && cd ../rag-gateway && ./mvnw -DskipTests package)"
      ],
      "priority": 9,
      "passes": true,
      "notes": "WebMvcConfig 补 excludePathPatterns(\"/api/ai/auth/**\")；AuthInterceptor 本身已经识别 @AnonymousAccess，path 白名单是 belt-and-suspenders。FeignConfiguration 新增 authHeaderRelayRequestInterceptor bean，从 ServletRequestAttributes 里捞 Authorization 和 X-Token，都按原名透传；非 web 线程（定时任务/MQ）时什么也不做。rag-gateway 新建 JwtAuthGlobalFilter（order=-100），对白名单 /api/ai/auth/** 放行，其他请求必须有 Authorization 或 X-Token 非空 header 才放行，缺失直接写回 401 + Result.error(UNAUTHORIZED) JSON body；不解析 token，最终判定仍在 user-service AuthInterceptor。rag-gateway/pom 加 rag-common 依赖（不要引 rag-common-service，会拖进 servlet-web 栈冲突 reactive）。verified: rag-common → rag-common-service → rag-gateway 三步 ./mvnw -DskipTests package 都退出 0；活体 401/200 校验留给 US-013 smoke。"
    },
    {
      "id": "US-010",
      "title": "Align embedding model between Java writer and Python reader",
      "description": "As an operator, I want a single embedding model serving both Java doc-processing writes and Python retrieval reads so vector dimensions match and retrieval recalls correctly.",
      "acceptanceCriteria": [
        "Pick option A or B and document the choice in commit body",
        "Option A: rag-document-service introduces an EmbeddingFactory mirroring Python EmbeddingsFactory (openai/dashscope/ollama), reading provider+model from nacos-config/rag-document-service.yaml; Java config keys must match Python settings keys",
        "Option B: Java stops calling vectorStore.add(); after writing chunks to MySQL it issues a synchronous HTTP POST to rag-ai-service /internal/embed-and-index with {kb_id, chunks[]}; Python receives, embeds, writes to ChromaDB collection kb_{kb_id}",
        "Either way: provider+model is configured in exactly one place per environment",
        "Manual verification: after processing a document via Java, POST /api/ai/knowledge-base/test-retrieval (Python) with a query from that document returns at least one hit with score > 0.5",
        "Build/Typecheck passes (cd rag-document-service && ./mvnw -DskipTests package && python -c \"from app.services.embedding.embedding_factory import EmbeddingsFactory\")"
      ],
      "priority": 10,
      "passes": true,
      "notes": "选 Option B：Python 单点持有 embedding 栈。Python 新增 app/api/internal/embed.py 暴露 POST /internal/embed-and-index（挂在主 app 的 /internal prefix 下，网关未配 route、不对外）；接 {kb_id, chunks:[{id,content,metadata}]}，用 EmbeddingsFactory + VectorStoreFactory 写到 ChromaDB `kb_{kb_id}`。Java 侧：新增 RagAiServiceClient FeignClient（name=rag-ai-service，走 Nacos）+ EmbedAndIndexRequest / EmbedAndIndexResponse DTO；DocumentProcessorService 把原先 `vectorStore.add(aiDocList)` 改成 `ragAiServiceClient.embedAndIndex(...)`，chunk id 用之前已经生成的 UUID，metadata 与 Python ChunkMetadata 字段对齐（source/kb_id/document_id/chunk_id + loader metadata putIfAbsent）。同步清理：删 AIConfig.java；pom 去掉 spring-ai-starter-vector-store-chroma + spring-ai-starter-model-openai；bootstrap.yml 去掉 spring.ai.vectorstore.chroma；nacos-config/rag-document-service.yaml 去掉 ai.embedding-model.api-key；DocumentProcessorService 清掉 unused import（VectorStore、org.springframework.ai.document.Document、Collectors）。verified: `cd rag-document-service && ./mvnw -DskipTests package` 退出 0；`uv run python -c 'from app.services.embedding.embedding_factory import EmbeddingsFactory; import app.main; from app.api.internal.embed import router'` 退出 0，router.routes 仅含 POST /embed-and-index；写-读同一 collection `kb_{kb_id}`。活体 test-retrieval score>0.5 校验留给 US-013。"
    },
    {
      "id": "US-011",
      "title": "Build all Java modules in dependency order via mvnw",
      "description": "As a developer, I want every Java module to compile and package locally so I can run them without a system mvn install.",
      "acceptanceCriteria": [
        "Run in order from rag-service/: (cd rag-common && ./mvnw clean install -DskipTests) && (cd rag-common-service && ./mvnw clean install -DskipTests) && (cd rag-user-service && ./mvnw clean package -DskipTests) && (cd rag-document-service && ./mvnw clean package -DskipTests) && (cd rag-knowledge-service && ./mvnw clean package -DskipTests) && (cd rag-gateway && ./mvnw clean package -DskipTests)",
        "Each business module produces a runnable Spring Boot fat jar in <module>/target/*.jar",
        "Add scripts/ralph/build-java.sh that runs the chained command above and exits non-zero on any failure",
        "Build/Typecheck passes (bash scripts/ralph/build-java.sh exits 0)"
      ],
      "priority": 11,
      "passes": true,
      "notes": "新增 scripts/ralph/build-java.sh：set -euo pipefail，REPO_ROOT 从脚本位置推算，按 PRD 顺序跑 common/common-service install + 4 个业务模块 package -DskipTests，末尾列出 4 个 fat jar 路径并校验存在。构建时发现根 pom 只 import 了 spring-boot-dependencies 没继承 spring-boot-starter-parent，`spring-boot-maven-plugin` 没有 version 也没 bind repackage goal，先前产的都是 ~20KB 瘦 jar；在根 pom 的 build/pluginManagement 里加上 spring-boot-maven-plugin（version=${spring.boot.version}）+ repackage execution，子模块引用 by artifactId 自动继承；同时把 rag-common（纯库模块，无 @SpringBootApplication）里的 spring-boot-maven-plugin 声明整段去掉，否则父 pom 绑的 repackage 会因找不到 main class 构建失败。verified: bash scripts/ralph/build-java.sh 退出 0，rag-user-service 62MB / rag-document-service 93MB / rag-knowledge-service 51MB / rag-gateway 53MB 四个 jar 的 MANIFEST 都含 `Main-Class: org.springframework.boot.loader.launch.JarLauncher` + 对应 Start-Class。"
    },
    {
      "id": "US-012",
      "title": "Validate startup order and Nacos service registration",
      "description": "As an operator, I want a single command/checklist to bring up the whole stack and verify all services registered with Nacos before any client traffic.",
      "acceptanceCriteria": [
        "Add scripts/ralph/start-all.sh that: (1) verifies docker compose -f docker-compose.dev.yml ps shows nacos/db/chromadb/minio healthy; (2) starts rag-ai-service in background (uvicorn on 8000); (3) starts each Java service in background using java -jar <module>/target/*.jar; (4) waits for each port to accept connections",
        "Add scripts/ralph/check-nacos.sh that calls Nacos open API GET /nacos/v1/ns/instance/list?serviceName=<name> for rag-ai-service, rag-user-service, rag-knowledge-service, rag-document-service, rag-gateway, and prints a green ✓ for each healthy instance",
        "Manual verification: bash scripts/ralph/check-nacos.sh prints 5 ✓; curl http://127.0.0.1:8080/api/ai/auth/info returns the gateway-routed user-service response (200 with token, 401 without)",
        "Build/Typecheck passes (bash -n scripts/ralph/start-all.sh && bash -n scripts/ralph/check-nacos.sh)"
      ],
      "priority": 12,
      "passes": true,
      "notes": "start-all.sh：set -euo pipefail；docker compose -f docker-compose.dev.yml ps 校验 nacos/db/chromadb/minio 都 Up；`uv run python -m uvicorn app.main:app --host 0.0.0.0 --port 8000` 起 rag-ai-service；按 ls target/<module>-*.jar 起 rag-user-service(8081)/rag-knowledge-service(8082)/rag-document-service(8083)/rag-gateway(8080)；pid/log 写到 .run/<module>.{pid,log}（已 gitignore）；`wait_port` 用 `nc -z` 兜底 bash /dev/tcp，每个服务 90s 超时。check-nacos.sh：先 POST /nacos/v1/auth/login 拿 accessToken（Nacos 2.x 开启鉴权必走），再 GET /nacos/v1/ns/instance/list?serviceName=<name>&accessToken=...，用 python3 -c（注意不要用 f-string，否则跟 shell 单引号里的 \\\" 冲突；改用 .format()）解析 hosts，任一实例 healthy=True 则打绿色 ✓；5 个服务全 healthy 才 exit 0。verified: bash -n 两个脚本都通过；bash check-nacos.sh 在服务未启动时正确返回 `0 instances registered` + exit 1（证明 auth/parse 链路正确），所有 ✓ 的活体校验需要人先 bash start-all.sh 再跑。.gitignore 加 `/.run/` 避免 pid/log 进 git。"
    },
    {
      "id": "US-013",
      "title": "End-to-end RAG smoke test through gateway:8080",
      "description": "As a tester, I want a single script to walk the full pipeline so I can prove the whole RAG flow is working post-microservice-split.",
      "acceptanceCriteria": [
        "Add scripts/ralph/e2e-smoke.sh that, against http://127.0.0.1:8080, performs: (1) POST /api/ai/auth/register a fresh user; (2) POST /api/ai/auth/token to capture token; (3) POST /knowledge-base to create a KB and capture kb_id; (4) PUT /knowledge-base/{kb_id}/documents/upload with a sample .md or .txt under fixtures/ to capture upload_id; (5) POST /api/ai/documents/process with the upload_id to capture task_id; (6) poll GET /api/ai/documents/process/{task_id} until status=COMPLETED (timeout 120s); (7) POST /api/ai/chat/... with a question grounded in the document and assert the response references the source",
        "Script exits 0 only when all 7 steps return success",
        "Add fixtures/sample.md with a few paragraphs of distinctive text so the chat answer can be checked",
        "Manual verification: bash scripts/ralph/e2e-smoke.sh exits 0 against a freshly started stack",
        "Build/Typecheck passes (bash -n scripts/ralph/e2e-smoke.sh)"
      ],
      "priority": 13,
      "passes": true,
      "notes": "fixtures/sample.md：引入几个在整个代码库里唯一的专有名词（Zephyr-7 / Calipso-Mirror-Omega / Dr. Nadia Okonkwo / Tenerife Meson Laboratory / muon-clustered plasma），chat 答案里出现任一即可判定 retrieval 打通。scripts/ralph/e2e-smoke.sh：7 步串行，关键细节（对照后端实际形状）：register 是 application/json + {username,password,email}，密码要过 `^(?=.*[A-Z])(?=.*[a-z])(?=.*\\d).{8,}$` 正则，脚本里用 `Ralph2026_${SUFFIX}`；login 是 `/api/ai/auth/token` + form-urlencoded（**不是** JSON），token 从 Result<UserLoginVO>.data.accessToken 读；KB 创建 POST /knowledge-base 返 Result<KnowledgeBaseVO>.data.id；upload 是 PUT multipart `file=@...;type=text/markdown`，返 data[0].uploadId；process 入参 `{knowledgeId, processDocItemDTOList:[{documentUploadId, skipProcess:false}]}`，返 data.tasks[0].taskId；poll 每 3s 一次 120s 上限，状态实际是小写 pending/processing/completed/failed；chat 两步：先 POST /api/ai/chat {title, knowledge_base_ids:[kb_id]} 拿 id，再 POST /api/ai/chat/{id}/messages {messages:[{role:user,content:...}]}，整个流式响应 grep EXPECTED_KEYWORDS_REGEX 任一命中即成功。辅助 json_get 用 python3 + env 传 body，走 dotted path（支持数字索引）。verified: bash -n scripts/ralph/e2e-smoke.sh 通过；完整 7 步活体跑需要人先 docker compose up -d → build-java.sh → start-all.sh → check-nacos.sh 全绿，以及 rag-ai-service/.env 配了可用的 OPENAI_API_KEY（chat 步依赖）；脚本本身不假设 stack 在跑，任一步非预期立即 exit 1 并打印 full body 便于定位。"
    },
    {
      "id": "US-014",
      "title": "Align frontend API paths to the new gateway",
      "description": "As a frontend user, I want the Next.js app to talk to the new gateway so the UI flow (login → KB → upload → chat) works in the browser.",
      "acceptanceCriteria": [
        "frontend/.env or frontend/.env.development sets NEXT_PUBLIC_API_URL=http://127.0.0.1:8080 (or http://127.0.0.1.nip.io if keeping nginx)",
        "Audit all fetch/axios calls in frontend/: KB calls go to /knowledge-base/** (the Java route), not /api/ai/knowledge-base/**",
        "Auth header name used by the frontend matches Java GConstants.JWT_TOKEN_HEADER (X-Token); update frontend interceptor/axios default header accordingly",
        "File upload uses multipart field name 'file' to match Java DocumentController @RequestParam(\"file\") on PUT /knowledge-base/{kb_id}/documents/upload",
        "Manual verification: in a browser at http://localhost:3000 — register, login, create KB, upload a file, ask a question, see answer with citations",
        "Verify in browser using dev-browser skill",
        "Build/Typecheck passes (cd frontend && npm run build OR npx tsc --noEmit)"
      ],
      "priority": 14,
      "passes": true,
      "notes": "frontend/.env.development 新建（NEXT_PUBLIC_API_URL=http://127.0.0.1:8080，不在 .gitignore 的 `frontend/.env` 精确匹配里，能入库）。src/lib/api.ts 的 BASE_URL 从硬写 '/api/ai' 改成 `process.env.NEXT_PUBLIC_API_URL ?? ''`，调用点自己写全路径。全局改路：KB CRUD 直接走 /knowledge-base（Java），chat/api-keys/auth 加 /api/ai/ 前缀（Python 或 Java /api/ai/auth）。关键修正：文档上传 POST→PUT（Java KnowledgeBaseController 写的是 @PutMapping），multipart field 'files'→'file'（Java @RequestParam(\"file\") List<MultipartFile>）。process/tasks 老端点在 US-007 随 Python 一起删，改指向 Java 的 /api/ai/documents/process（payload reshape 成 ProcessDocDTO）+ /api/ai/documents/process/{taskId}（Result<String>，批量查改成并发单查再合并 map）。Auth header：PRD 笔误写成 X-Token，实际 Java GConstants.JWT_TOKEN_HEADER = 'Authorization'，沿用原来的 `Authorization: Bearer ${token}`（JWTUtil 会 strip Bearer 前缀）。遗留坑（加 TODO 注释，留给 follow-up story）：preview / citation document 详情 / test-retrieval 端点现在没有对应 Java/Python 后端路由，前端仍按旧路径调但会 404/405；Java Result wrapper 与 frontend 默认解包 shape 也不一致（另一类 story）。verified: `npm install` (使用 package.json，规避 pnpm-lock 版本冲突) + `npx tsc --noEmit` 无输出（pass）；浏览器 e2e 校验（dev-browser skill）需人先起 stack + frontend dev server 再做。"
    }
  ]
}
